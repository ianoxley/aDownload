{
  "name": "aDownload",
  "version": "1.0",
  "description": "Uses the HTML5 download attribute to enable downloading of PDF and other supported files rather than rendering them in the browser.",
  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "options.html"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["config.js", "adownload.js"]
    }
  ],
  "options_page": "options.html"
}

